# Game event XML API reference

> Generated by `Tools/generate_game_event_docs.py`. Do not edit this file manually.
> Syntax comes from `game-events.xsd`; trigger arguments come from the runtime trigger registry.

This reference lists every currently accepted event element and its serialized shape. See
[Creating custom game events](GameEvents.md) for lifecycle semantics and complete recipes.

## Event definition

### `GameEvent`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `TriggerCount` | No | `xs:positiveInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `InstanceID` | Exactly one | `xs:string`; minLength `1` |
| `Triggers` | Optional | `TriggersType` |
| `Schedule` | Optional | `GameEventSchedulerType` |
| `Conditionals` | Optional | `ConditionalsType` |
| `Until` | Optional | `ConditionalsType` |
| `Target` | Optional | `TargetType` |
| `Actions` | Optional | `ActionsType` |

## Scheduling

### `At`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Tick` | Yes | `xs:nonNegativeInteger` |

### `Every`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Ticks` | Yes | `xs:positiveInteger` |
| `InitialDelayTicks` | No | `xs:nonNegativeInteger` |

### `Random`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `MinimumTicks` | Yes | `xs:positiveInteger` |
| `MaximumTicks` | Yes | `xs:positiveInteger` |

### `After`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `EventInstanceID` | Yes | `xs:string`; minLength `1` |
| `DelayTicks` | Yes | `xs:nonNegativeInteger` |

### `AfterAll`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `DelayTicks` | Yes | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Events` | Exactly one | Inline: `Event` (1 or more) |

### `AfterAny`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `DelayTicks` | Yes | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Events` | Exactly one | Inline: `Event` (1 or more) |

## Trigger contracts

### `core:duel.completed`

Runtime result: `DuelResult`

| Argument | Runtime value source |
| --- | --- |
| `Officer` | `result.EncounteredOfficer` |
| `OfficerInstanceID` | `result.EncounteredOfficer?.InstanceID` |
| `Opponent` | `result.OpposingOfficer` |
| `OpponentInstanceID` | `result.OpposingOfficer?.InstanceID` |
| `Location` | `result.Location` |
| `OfficerCaptured` | `result.EncounteredOfficerCaptured` |
| `OfficerInjury` | `result.EncounteredOfficerInjury` |
| `OpponentInjury` | `result.OpposingOfficerInjury` |
| `ImagePath` | `result.ImagePath` |
| `AudioPath` | `result.AudioPath` |
| `SourceEventInstanceID` | `result.SourceEventInstanceID` |

### `core:force.discovered`

Runtime result: `ForceDiscoveryResult`

| Argument | Runtime value source |
| --- | --- |
| `Officer` | `result.Officer` |
| `Discoverer` | `result.Discoverer` |
| `ForceRank` | `result.ForceRank` |
| `SourceEventInstanceID` | `result.SourceEventInstanceID` |

### `core:mission.completed`

Runtime result: `MissionCompletedResult`

| Argument | Runtime value source |
| --- | --- |
| `Mission` | `result.Mission` |
| `Outcome` | `result.Outcome` |
| `CompletionReason` | `result.CompletionReason` |
| `Participants` | `result.Participants` |
| `Location` | `result.Location` |
| `ReturnDestination` | `result.ReturnDestination` |
| `SourceEventInstanceID` | `result.SourceEventInstanceID` |

### `core:officer.capture-changed`

Runtime result: `OfficerCaptureStateResult`

| Argument | Runtime value source |
| --- | --- |
| `Officer` | `result.TargetOfficer ?? result.CapturedOfficer` |
| `LinkedOfficer` | `result.LinkedOfficer` |
| `Context` | `result.Context` |
| `IsCaptured` | `result.IsCaptured` |
| `SourceEventInstanceID` | `result.SourceEventInstanceID` |

### `core:unit.arrived`

Runtime result: `UnitArrivedResult`

| Argument | Runtime value source |
| --- | --- |
| `Unit` | `result.Unit` |
| `UnitInstanceID` | `result.Unit?.InstanceID` |
| `Destination` | `result.Destination` |
| `DestinationInstanceID` | `result.Destination?.InstanceID` |
| `SourceEventInstanceID` | `result.SourceEventInstanceID` |

## Conditions

### `All`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Conditionals` | Exactly one | Inline: `All` (Exactly one), `Any` (Exactly one), `Not` (Exactly one), `Xor` (Exactly one), `ShareParent` (Exactly one), `ShareAncestor` (Exactly one), `AreOnOpposingFactions` (Exactly one), `IsOnMission` (Exactly one), `IsInTransit` (Exactly one), `TickCount` (Exactly one), `HasEventTriggered` (Exactly one), `IsEventExhausted` (Exactly one), `EvaluateEventVariable` (Exactly one), `IsAtLocation` (Exactly one), `IsOwned` (Exactly one), `RollAgainstPopularSupport` (Exactly one), `EvaluateBinding` (Exactly one), `BindingIncludesUnit` (Exactly one), `IsCaptured` (Exactly one), `IsKilled` (Exactly one), `IsInjured` (Exactly one), `IsForceEligible` (Exactly one), `IsCapturedBy` (Exactly one), `HasForceRank` (Exactly one), `CompareOfficerRating` (Exactly one), `CompareOfficerForce` (Exactly one), `ComparePlanetStat` (Exactly one), `HasBuildingType` (Exactly one) |

### `Any`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Conditionals` | Exactly one | Inline: `All` (Exactly one), `Any` (Exactly one), `Not` (Exactly one), `Xor` (Exactly one), `ShareParent` (Exactly one), `ShareAncestor` (Exactly one), `AreOnOpposingFactions` (Exactly one), `IsOnMission` (Exactly one), `IsInTransit` (Exactly one), `TickCount` (Exactly one), `HasEventTriggered` (Exactly one), `IsEventExhausted` (Exactly one), `EvaluateEventVariable` (Exactly one), `IsAtLocation` (Exactly one), `IsOwned` (Exactly one), `RollAgainstPopularSupport` (Exactly one), `EvaluateBinding` (Exactly one), `BindingIncludesUnit` (Exactly one), `IsCaptured` (Exactly one), `IsKilled` (Exactly one), `IsInjured` (Exactly one), `IsForceEligible` (Exactly one), `IsCapturedBy` (Exactly one), `HasForceRank` (Exactly one), `CompareOfficerRating` (Exactly one), `CompareOfficerForce` (Exactly one), `ComparePlanetStat` (Exactly one), `HasBuildingType` (Exactly one) |

### `Not`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Conditionals` | Exactly one | Inline: `All` (Exactly one), `Any` (Exactly one), `Not` (Exactly one), `Xor` (Exactly one), `ShareParent` (Exactly one), `ShareAncestor` (Exactly one), `AreOnOpposingFactions` (Exactly one), `IsOnMission` (Exactly one), `IsInTransit` (Exactly one), `TickCount` (Exactly one), `HasEventTriggered` (Exactly one), `IsEventExhausted` (Exactly one), `EvaluateEventVariable` (Exactly one), `IsAtLocation` (Exactly one), `IsOwned` (Exactly one), `RollAgainstPopularSupport` (Exactly one), `EvaluateBinding` (Exactly one), `BindingIncludesUnit` (Exactly one), `IsCaptured` (Exactly one), `IsKilled` (Exactly one), `IsInjured` (Exactly one), `IsForceEligible` (Exactly one), `IsCapturedBy` (Exactly one), `HasForceRank` (Exactly one), `CompareOfficerRating` (Exactly one), `CompareOfficerForce` (Exactly one), `ComparePlanetStat` (Exactly one), `HasBuildingType` (Exactly one) |

### `Xor`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Conditionals` | Exactly one | Inline: `All` (Exactly one), `Any` (Exactly one), `Not` (Exactly one), `Xor` (Exactly one), `ShareParent` (Exactly one), `ShareAncestor` (Exactly one), `AreOnOpposingFactions` (Exactly one), `IsOnMission` (Exactly one), `IsInTransit` (Exactly one), `TickCount` (Exactly one), `HasEventTriggered` (Exactly one), `IsEventExhausted` (Exactly one), `EvaluateEventVariable` (Exactly one), `IsAtLocation` (Exactly one), `IsOwned` (Exactly one), `RollAgainstPopularSupport` (Exactly one), `EvaluateBinding` (Exactly one), `BindingIncludesUnit` (Exactly one), `IsCaptured` (Exactly one), `IsKilled` (Exactly one), `IsInjured` (Exactly one), `IsForceEligible` (Exactly one), `IsCapturedBy` (Exactly one), `HasForceRank` (Exactly one), `CompareOfficerRating` (Exactly one), `CompareOfficerForce` (Exactly one), `ComparePlanetStat` (Exactly one), `HasBuildingType` (Exactly one) |

### `ShareParent`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Units` | Exactly one | Inline: `Unit` (2 or more) |

### `ShareAncestor`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Type` | Yes | `Galaxy`, `PlanetSystem`, `Planet`, `Fleet`, `Mission`, `CapitalShip` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Units` | Exactly one | Inline: `Unit` (2 or more) |

### `AreOnOpposingFactions`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `UnitInstanceIDs` | Exactly one | `StringListType` |

### `IsOnMission`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | Yes | `xs:string`; minLength `1` |

### `IsInTransit`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | Yes | `xs:string`; minLength `1` |

### `TickCount`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Ticks` | Yes | `xs:nonNegativeInteger` |

### `HasEventTriggered`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `EventInstanceID` | Yes | `xs:string`; minLength `1` |

### `IsEventExhausted`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `EventInstanceID` | Yes | `xs:string`; minLength `1` |

### `EvaluateEventVariable`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Key` | Yes | `xs:string`; minLength `1` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `CompareTo` | Yes | `xs:integer` |

### `IsAtLocation`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `UnitInstanceID` | Exactly one | `xs:string`; minLength `1` |
| `LocationInstanceID` | Exactly one | `xs:string`; minLength `1` |

### `IsOwned`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `FactionInstanceID` | No | `xs:string`; minLength `1` |

### `RollAgainstPopularSupport`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `FactionInstanceID` | Yes | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |

### `EvaluateBinding`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Binding` | Yes | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `CompareTo` | Yes | `xs:string`; minLength `1` |

### `BindingIncludesUnit`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Binding` | Yes | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `UnitInstanceID` | Yes | `xs:string`; minLength `1` |

### `IsCaptured`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

### `IsKilled`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

### `IsInjured`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

### `IsForceEligible`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

### `IsCapturedBy`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `CaptorFactionInstanceID` | Yes | `xs:string`; minLength `1` |

### `HasForceRank`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Rank` | Yes | `None`, `Novice`, `Trainee`, `ForceStudent`, `ForceKnight`, `ForceMaster` |

### `CompareOfficerRating`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `Rating` | Yes | `Diplomacy`, `Espionage`, `Combat`, `Leadership`, `ShipResearch`, `TroopResearch`, `FacilityResearch` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Value` | Yes | `xs:int` |

### `CompareOfficerForce`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Value` | Yes | `xs:int` |

### `ComparePlanetStat`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `Stat` | Yes | `RawResourceNodes`, `EnergyCapacity` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Value` | Yes | `xs:int` |

### `HasBuildingType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Type` | Yes | `Mine`, `Refinery`, `Shipyard`, `TrainingFacility`, `ConstructionFacility`, `Defense`, `Weapon`, `Headquarters` |

## Selectors

### `SelectAncestors`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Type` | Yes | `Galaxy`, `PlanetSystem`, `Planet`, `Fleet`, `Mission`, `CapitalShip` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectPlanets` (Exactly one), `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectMissions` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `SelectBinding`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Binding` | Yes | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |

### `SelectBuildings`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `TypeID` | No | `xs:string`; minLength `1` |
| `ManufacturingStatus` | No | `Building`, `Complete` |
| `Category` | No | `Any`, `PlanetaryDefense`, `ManufacturingFacility` |

### `SelectCapitalShips`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `TypeID` | No | `xs:string`; minLength `1` |
| `ManufacturingStatus` | No | `Building`, `Complete` |

### `SelectFirst`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectPlanets` (Exactly one), `SelectFleets` (Exactly one), `SelectMissions` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectBinding` (Exactly one), `SelectAncestors` (Exactly one), `SelectPreviousLocation` (Exactly one) |

### `SelectFleets`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |

### `SelectMissions`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |

### `SelectOfficers`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `IsCaptured` | No | `xs:boolean` |
| `IncludeRetained` | No | `xs:boolean` |

### `SelectPlanets`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `SystemType` | No | `CoreSystem`, `OuterRim` |

### `SelectPreviousLocation`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | No | `xs:string`; minLength `1` |
| `UnitBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |

### `SelectRandom`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `ChancePercent` | No | `xs:integer`; minInclusive `0`; maxInclusive `100` |
| `Count` | No | `xs:nonNegativeInteger` |
| `MinimumCount` | No | `xs:nonNegativeInteger` |
| `MaximumCount` | No | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `SelectRegiments`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `TypeID` | No | `xs:string`; minLength `1` |
| `ManufacturingStatus` | No | `Building`, `Complete` |

### `SelectSpecialForces`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |

### `SelectStarfighters`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `TypeID` | No | `xs:string`; minLength `1` |
| `ManufacturingStatus` | No | `Building`, `Complete` |

## Actions

### `RevealToFaction`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `FactionInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Subjects` | Exactly one | Inline: `SelectPlanets` (Exactly one), `SelectPlanetSystems` (Exactly one), `SelectFleets` (Exactly one), `SelectMissions` (Exactly one), `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectManufacturingOrders` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `ChangePlanetStat`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `Stat` | Yes | `RawResourceNodes`, `EnergyCapacity` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Planets` | Optional | Inline: `SelectPlanets` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `ReducePlanetStats`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `LossProbabilityPerResource` | Yes | `xs:decimal`; minInclusive `0`; maxInclusive `1` |
| `MinimumTotalLoss` | Yes | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Stats` | Exactly one | Inline: `Stat` (1 or more) |

### `RecordPlanetIncident`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Type` | Yes | `xs:string`; minLength `1` |

### `DestroyUnits`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Units` | Exactly one | Inline: `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `Random`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Outcomes` | Exactly one | Inline: `Outcome` (1 or more) |

### `TriggerDuel`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `FirstOfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `SecondOfficerInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `ImagePath` | Optional | `xs:string`; minLength `1` |
| `AudioPath` | Optional | `xs:string`; minLength `1` |

### `SendMessage`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `RecipientFactionInstanceID` | No | `xs:string`; minLength `1` |
| `RecipientUnitInstanceID` | No | `xs:string`; minLength `1` |
| `SubjectInstanceID` | No | `xs:string`; minLength `1` |
| `SubjectBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `RelatedSubjectInstanceID` | No | `xs:string`; minLength `1` |
| `LocationInstanceID` | No | `xs:string`; minLength `1` |
| `LocationBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `Type` | No | `PopularSupport`, `Fleet`, `Mission`, `Resource`, `Manufacturing`, `Defense`, `Conflict`, `Chat`, `Advice` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Subject` | Optional | `xs:string` |
| `Body` | Optional | `xs:string` |
| `ConditionalBodies` | Optional | `ConditionalMessageBodyListType` |
| `BackgroundImage` | Optional | `BackgroundImageType` |
| `OverlayImage` | Optional | `MessageImageType` |
| `BackgroundAudio` | Optional | `MessageAudioType` |
| `OfficerVoice` | Optional | `OfficerVoiceType` |
| `AdvisorNotification` | Optional | `AdvisorNotificationType` |

### `If`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Conditions` | Exactly one | `ConditionalsType` |
| `Actions` | Exactly one | `ActionsType` |
| `Else` | Optional | `ActionsType` |

### `SetEventVariable`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Key` | Exactly one | `xs:string`; minLength `1` |
| `Operation` | Optional | `Set`, `Add`, `Minimum`, `Maximum` |
| `Operand` | Exactly one | `xs:integer` |

### `SetOfficerImages`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `DisplayImagePath` | Optional | `xs:string`; minLength `1` |
| `SmallDisplayImagePath` | Optional | `xs:string`; minLength `1` |
| `MessageImagePath` | Optional | `xs:string`; minLength `1` |
| `EncyclopediaImagePath` | Optional | `xs:string`; minLength `1` |

### `SetOfficerVoiceSet`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Order` | Optional | `PathListType` |
| `PersonnelArrived` | Optional | `PathListType` |
| `MissionSuccess` | Optional | `PathListType` |
| `MissionFailure` | Optional | `PathListType` |
| `MissionAbort` | Optional | `PathListType` |
| `Released` | Optional | `PathListType` |
| `Recovered` | Optional | `PathListType` |
| `EnemyDetected` | Optional | `PathListType` |
| `ForceGrowth` | Optional | `PathListType` |
| `ForceUserDiscovered` | Optional | `PathListType` |
| `TraitorDiscovered` | Optional | `PathListType` |
| `RescueAttempt` | Optional | `PathListType` |

### `PlaceUnits`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | No | `xs:string`; minLength `1` |
| `DestinationInstanceID` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Units` | Optional | `MovableSelectorsType` |
| `Destination` | Optional | `DestinationSelectorsType` |

### `SendUnits`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | No | `xs:string`; minLength `1` |
| `DestinationInstanceID` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Units` | Optional | `MovableSelectorsType` |
| `Destination` | Optional | `DestinationSelectorsType` |

### `AddToVoid`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `UnitInstanceID` | Optional | `xs:string`; minLength `1` |
| `Units` | Optional | Inline: `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `SetDisplayName`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `TargetInstanceID` | No | `xs:string`; minLength `1` |
| `Name` | No | `xs:string`; minLength `1` |
| `Status` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Targets` | Optional | Inline: `SelectPlanets` (Exactly one), `SelectPlanetSystems` (Exactly one), `SelectOfficers` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `SetDisplayStatus`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `TargetInstanceID` | No | `xs:string`; minLength `1` |
| `Name` | No | `xs:string`; minLength `1` |
| `Status` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Targets` | Optional | Inline: `SelectPlanets` (Exactly one), `SelectPlanetSystems` (Exactly one), `SelectOfficers` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `ClearDisplayStatus`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `TargetInstanceID` | No | `xs:string`; minLength `1` |
| `Name` | No | `xs:string`; minLength `1` |
| `Status` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Targets` | Optional | Inline: `SelectPlanets` (Exactly one), `SelectPlanetSystems` (Exactly one), `SelectOfficers` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `ChangeOfficerRating`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | No | `xs:string`; minLength `1` |
| `Rating` | Yes | `Diplomacy`, `Espionage`, `Combat`, `Leadership`, `ShipResearch`, `TroopResearch`, `FacilityResearch` |
| `ReferenceOfficerInstanceID` | No | `xs:string`; minLength `1` |
| `MinimumAmount` | No | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Officers` | Optional | Inline: `SelectOfficers` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `IncreaseOfficerForce`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | No | `xs:string`; minLength `1` |
| `ReferenceOfficerInstanceID` | No | `xs:string`; minLength `1` |
| `MinimumAmount` | No | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Officers` | Optional | Inline: `SelectOfficers` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `PerformSkillCheck`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `Rating` | Yes | `Diplomacy`, `Espionage`, `Combat`, `Leadership`, `ShipResearch`, `TroopResearch`, `FacilityResearch` |
| `ProbabilityTable` | Yes | `xs:string`; minLength `1` |
| `RatingMultiplier` | No | `xs:int` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `OnSuccess` | Optional | `ActionsType` |
| `OnFailure` | Optional | `ActionsType` |

### `RemoveFromVoid`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `UnitInstanceID` | Optional | `xs:string`; minLength `1` |
| `Units` | Optional | Inline: `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `SetForceSensitive`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

### `SetForceEligible`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

### `ApplyOfficerInjury`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `MinimumInjury` | Exactly one | `xs:nonNegativeInteger` |
| `MaximumInjury` | Exactly one | `xs:nonNegativeInteger` |

### `SetCaptureStatus`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | No | `xs:string`; minLength `1` |
| `IsCaptured` | Yes | `xs:boolean` |
| `CaptorFactionInstanceID` | No | `xs:string`; minLength `1` |
| `CanEscape` | No | `xs:boolean` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Officers` | Optional | Inline: `SelectOfficers` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

## Enumerated values

### `BuildingSelectionCategoryType`

`Any`, `PlanetaryDefense`, `ManufacturingFacility`

### `BuildingTypeType`

`Mine`, `Refinery`, `Shipyard`, `TrainingFacility`, `ConstructionFacility`, `Defense`, `Weapon`, `Headquarters`

### `ComparisonType`

`Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual`

### `EventVariableOperationType`

`Set`, `Add`, `Minimum`, `Maximum`

### `ForceRankLabelType`

`None`, `Novice`, `Trainee`, `ForceStudent`, `ForceKnight`, `ForceMaster`

### `GameEventTriggerIDType`

`core:unit.arrived`, `core:duel.completed`, `core:officer.capture-changed`, `core:mission.completed`, `core:force.discovered`

### `ManufacturingStatusType`

`Building`, `Complete`

### `ManufacturingTypeType`

`Ship`, `Building`, `Troop`

### `MessageTypeType`

`PopularSupport`, `Fleet`, `Mission`, `Resource`, `Manufacturing`, `Defense`, `Conflict`, `Chat`, `Advice`

### `OfficerRatingType`

`Diplomacy`, `Espionage`, `Combat`, `Leadership`, `ShipResearch`, `TroopResearch`, `FacilityResearch`

### `OfficerVoicePresetType`

`Order`, `PersonnelArrived`, `MissionSuccess`, `MissionFailure`, `MissionAbort`, `Released`, `Recovered`, `EnemyDetected`, `ForceGrowth`, `ForceUserDiscovered`, `TraitorDiscovered`, `RescueAttempt`

### `PlanetStatType`

`RawResourceNodes`, `EnergyCapacity`

### `PlanetSystemTypeType`

`CoreSystem`, `OuterRim`

### `SceneAncestorType`

`Galaxy`, `PlanetSystem`, `Planet`, `Fleet`, `Mission`, `CapitalShip`

## Named schema types

These definitions expand the named payload types referenced by the element tables above.

### `ActionsType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `RevealToFaction` | 0 or more | `RevealToFactionActionType` |
| `ChangePlanetStat` | 0 or more | `ChangePlanetStatActionType` |
| `ReducePlanetStats` | 0 or more | `ReducePlanetStatsActionType` |
| `RecordPlanetIncident` | 0 or more | `RecordPlanetIncidentActionType` |
| `DestroyUnits` | 0 or more | `DestroyUnitsActionType` |
| `Random` | 0 or more | `RandomActionType` |
| `TriggerDuel` | 0 or more | `TriggerDuelActionType` |
| `SendMessage` | 0 or more | `SendMessageActionType` |
| `If` | 0 or more | `IfActionType` |
| `SetEventVariable` | 0 or more | `SetEventVariableActionType` |
| `SetOfficerImages` | 0 or more | `SetOfficerImagesActionType` |
| `SetOfficerVoiceSet` | 0 or more | `SetOfficerVoiceSetActionType` |
| `PlaceUnits` | 0 or more | `UnitTransferActionType` |
| `SendUnits` | 0 or more | `UnitTransferActionType` |
| `AddToVoid` | 0 or more | `VoidTransferActionType` |
| `SetDisplayName` | 0 or more | `DisplayActionType` |
| `SetDisplayStatus` | 0 or more | `DisplayActionType` |
| `ClearDisplayStatus` | 0 or more | `DisplayActionType` |
| `ChangeOfficerRating` | 0 or more | `ChangeOfficerRatingActionType` |
| `IncreaseOfficerForce` | 0 or more | `IncreaseOfficerForceActionType` |
| `PerformSkillCheck` | 0 or more | `PerformSkillCheckActionType` |
| `RemoveFromVoid` | 0 or more | `VoidTransferActionType` |
| `SetForceSensitive` | 0 or more | `OfficerActionType` |
| `SetForceEligible` | 0 or more | `OfficerActionType` |
| `ApplyOfficerInjury` | 0 or more | `ApplyOfficerInjuryActionType` |
| `SetCaptureStatus` | 0 or more | `SetCaptureStatusActionType` |

### `AdvisorAnimationType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Animation` | No | `xs:string`; minLength `1` |
| `AnimationPath` | No | `xs:string`; minLength `1` |
| `FrameCount` | No | `xs:positiveInteger` |
| `Audio` | No | `xs:string`; minLength `1` |
| `AudioPath` | No | `xs:string`; minLength `1` |
| `DelayBeforeSeconds` | No | `xs:decimal`; minInclusive `0` |
| `RequiresAnnouncementsEnabled` | No | `xs:boolean` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Animation` | Optional | `xs:string`; minLength `1` |
| `AnimationPath` | Optional | `xs:string`; minLength `1` |
| `FrameCount` | Optional | `xs:positiveInteger` |
| `Audio` | Optional | `xs:string`; minLength `1` |
| `AudioPath` | Optional | `xs:string`; minLength `1` |
| `DelayBeforeSeconds` | Optional | `xs:decimal`; minInclusive `0` |
| `RequiresAnnouncementsEnabled` | Optional | `xs:boolean` |

### `AdvisorNotificationType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Preset` | No | `xs:string`; minLength `1` |
| `LifetimeTicks` | No | `xs:positiveInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Droid` | Optional | `AdvisorAnimationType` |
| `Protocol` | Optional | `AdvisorAnimationType` |
| `Preset` | Optional | `xs:string`; minLength `1` |
| `LifetimeTicks` | Optional | `xs:positiveInteger` |

### `AfterEventType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `EventInstanceID` | Yes | `xs:string`; minLength `1` |
| `DelayTicks` | Yes | `xs:nonNegativeInteger` |

### `AfterEventsType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `DelayTicks` | Yes | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Events` | Exactly one | Inline: `Event` (1 or more) |

### `ApplyOfficerInjuryActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `MinimumInjury` | Exactly one | `xs:nonNegativeInteger` |
| `MaximumInjury` | Exactly one | `xs:nonNegativeInteger` |

### `AtTickType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Tick` | Yes | `xs:nonNegativeInteger` |

### `BackgroundImageType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Key` | No | `xs:string`; minLength `1` |
| `Path` | No | `xs:string`; minLength `1` |
| `Binding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Key` | Optional | `xs:string`; minLength `1` |
| `Path` | Optional | `xs:string`; minLength `1` |

### `BindingIncludesUnitConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Binding` | Yes | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `UnitInstanceID` | Yes | `xs:string`; minLength `1` |

### `ChangeOfficerRatingActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | No | `xs:string`; minLength `1` |
| `Rating` | Yes | `Diplomacy`, `Espionage`, `Combat`, `Leadership`, `ShipResearch`, `TroopResearch`, `FacilityResearch` |
| `ReferenceOfficerInstanceID` | No | `xs:string`; minLength `1` |
| `MinimumAmount` | No | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Officers` | Optional | Inline: `SelectOfficers` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `ChangePlanetStatActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `Stat` | Yes | `RawResourceNodes`, `EnergyCapacity` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Planets` | Optional | Inline: `SelectPlanets` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `CompareOfficerForceConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Value` | Yes | `xs:int` |

### `CompareOfficerRatingConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `Rating` | Yes | `Diplomacy`, `Espionage`, `Combat`, `Leadership`, `ShipResearch`, `TroopResearch`, `FacilityResearch` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Value` | Yes | `xs:int` |

### `ComparePlanetStatConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `Stat` | Yes | `RawResourceNodes`, `EnergyCapacity` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Value` | Yes | `xs:int` |

### `ConditionalGroupType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Conditionals` | Exactly one | Inline: `All` (Exactly one), `Any` (Exactly one), `Not` (Exactly one), `Xor` (Exactly one), `ShareParent` (Exactly one), `ShareAncestor` (Exactly one), `AreOnOpposingFactions` (Exactly one), `IsOnMission` (Exactly one), `IsInTransit` (Exactly one), `TickCount` (Exactly one), `HasEventTriggered` (Exactly one), `IsEventExhausted` (Exactly one), `EvaluateEventVariable` (Exactly one), `IsAtLocation` (Exactly one), `IsOwned` (Exactly one), `RollAgainstPopularSupport` (Exactly one), `EvaluateBinding` (Exactly one), `BindingIncludesUnit` (Exactly one), `IsCaptured` (Exactly one), `IsKilled` (Exactly one), `IsInjured` (Exactly one), `IsForceEligible` (Exactly one), `IsCapturedBy` (Exactly one), `HasForceRank` (Exactly one), `CompareOfficerRating` (Exactly one), `CompareOfficerForce` (Exactly one), `ComparePlanetStat` (Exactly one), `HasBuildingType` (Exactly one) |

### `ConditionalMessageBodyListType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `ConditionalBody` | 0 or more | `ConditionalMessageBodyType` |

### `ConditionalMessageBodyType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Conditions` | Optional | `ConditionalsType` |
| `Body` | Optional | `xs:string` |
| `ElseBody` | Optional | `xs:string` |

### `ConditionalsType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `All` | 0 or more | `ConditionalGroupType` |
| `Any` | 0 or more | `ConditionalGroupType` |
| `Not` | 0 or more | `ConditionalGroupType` |
| `Xor` | 0 or more | `ConditionalGroupType` |
| `ShareParent` | 0 or more | `SharedNodeConditionalType` |
| `ShareAncestor` | 0 or more | `SharedAncestorConditionalType` |
| `AreOnOpposingFactions` | 0 or more | `StringListConditionalType` |
| `IsOnMission` | 0 or more | `UnitConditionalType` |
| `IsInTransit` | 0 or more | `UnitConditionalType` |
| `TickCount` | 0 or more | `TickCountConditionalType` |
| `HasEventTriggered` | 0 or more | `EventReferenceConditionalType` |
| `IsEventExhausted` | 0 or more | `EventReferenceConditionalType` |
| `EvaluateEventVariable` | 0 or more | `EventVariableConditionalType` |
| `IsAtLocation` | 0 or more | `IsAtLocationConditionalType` |
| `IsOwned` | 0 or more | `IsOwnedConditionalType` |
| `RollAgainstPopularSupport` | 0 or more | `PopularSupportRollConditionalType` |
| `EvaluateBinding` | 0 or more | `EvaluateBindingConditionalType` |
| `BindingIncludesUnit` | 0 or more | `BindingIncludesUnitConditionalType` |
| `IsCaptured` | 0 or more | `OfficerConditionalType` |
| `IsKilled` | 0 or more | `OfficerConditionalType` |
| `IsInjured` | 0 or more | `OfficerConditionalType` |
| `IsForceEligible` | 0 or more | `OfficerConditionalType` |
| `IsCapturedBy` | 0 or more | `IsCapturedByConditionalType` |
| `HasForceRank` | 0 or more | `OfficerForceRankConditionalType` |
| `CompareOfficerRating` | 0 or more | `CompareOfficerRatingConditionalType` |
| `CompareOfficerForce` | 0 or more | `CompareOfficerForceConditionalType` |
| `ComparePlanetStat` | 0 or more | `ComparePlanetStatConditionalType` |
| `HasBuildingType` | 0 or more | `HasBuildingTypeConditionalType` |

### `DestinationSelectorsType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `SelectPlanets` | 0 or more | `SelectPlanetsType` |
| `SelectFleets` | 0 or more | `LocatedSelectorType` |
| `SelectMissions` | 0 or more | `LocatedSelectorType` |
| `SelectCapitalShips` | 0 or more | `SelectManufacturableType` |
| `SelectBinding` | 0 or more | `SelectBindingType` |
| `SelectAncestors` | 0 or more | `SelectAncestorsType` |
| `SelectPreviousLocation` | 0 or more | `SelectPreviousLocationType` |
| `SelectFirst` | 0 or more | `SelectFirstDestinationType` |

### `DestroyUnitsActionType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Units` | Exactly one | Inline: `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `DisplayActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `TargetInstanceID` | No | `xs:string`; minLength `1` |
| `Name` | No | `xs:string`; minLength `1` |
| `Status` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Targets` | Optional | Inline: `SelectPlanets` (Exactly one), `SelectPlanetSystems` (Exactly one), `SelectOfficers` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `EvaluateBindingConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Binding` | Yes | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `CompareTo` | Yes | `xs:string`; minLength `1` |

### `EventDependencyType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `EventInstanceID` | Yes | `xs:string`; minLength `1` |

### `EventReferenceConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `EventInstanceID` | Yes | `xs:string`; minLength `1` |

### `EventUnitReferenceType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | Yes | `xs:string`; minLength `1` |

### `EventVariableConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Key` | Yes | `xs:string`; minLength `1` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `CompareTo` | Yes | `xs:integer` |

### `EveryTicksType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Ticks` | Yes | `xs:positiveInteger` |
| `InitialDelayTicks` | No | `xs:nonNegativeInteger` |

### `GameEventSchedulerType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `At` | Exactly one | `AtTickType` |
| `Every` | Exactly one | `EveryTicksType` |
| `Random` | Exactly one | `RandomTickRangeType` |
| `After` | Exactly one | `AfterEventType` |
| `AfterAll` | Exactly one | `AfterEventsType` |
| `AfterAny` | Exactly one | `AfterEventsType` |

### `GameEventType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `TriggerCount` | No | `xs:positiveInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `InstanceID` | Exactly one | `xs:string`; minLength `1` |
| `Triggers` | Optional | `TriggersType` |
| `Schedule` | Optional | `GameEventSchedulerType` |
| `Conditionals` | Optional | `ConditionalsType` |
| `Until` | Optional | `ConditionalsType` |
| `Target` | Optional | `TargetType` |
| `Actions` | Optional | `ActionsType` |

### `HasBuildingTypeConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Type` | Yes | `Mine`, `Refinery`, `Shipyard`, `TrainingFacility`, `ConstructionFacility`, `Defense`, `Weapon`, `Headquarters` |

### `IfActionType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Conditions` | Exactly one | `ConditionalsType` |
| `Actions` | Exactly one | `ActionsType` |
| `Else` | Optional | `ActionsType` |

### `IncreaseOfficerForceActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | No | `xs:string`; minLength `1` |
| `ReferenceOfficerInstanceID` | No | `xs:string`; minLength `1` |
| `MinimumAmount` | No | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Officers` | Optional | Inline: `SelectOfficers` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `IsAtLocationConditionalType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `UnitInstanceID` | Exactly one | `xs:string`; minLength `1` |
| `LocationInstanceID` | Exactly one | `xs:string`; minLength `1` |

### `IsCapturedByConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `CaptorFactionInstanceID` | Yes | `xs:string`; minLength `1` |

### `IsOwnedConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `FactionInstanceID` | No | `xs:string`; minLength `1` |

### `LocatedSelectorType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |

### `MessageAudioType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Path` | No | `xs:string`; minLength `1` |
| `Binding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Path` | Optional | `xs:string`; minLength `1` |

### `MessageImageType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Path` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Path` | Optional | `xs:string`; minLength `1` |

### `MovableSelectorsType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `SelectOfficers` | 0 or more | `SelectOfficersType` |
| `SelectSpecialForces` | 0 or more | `LocatedSelectorType` |
| `SelectFleets` | 0 or more | `LocatedSelectorType` |
| `SelectCapitalShips` | 0 or more | `SelectManufacturableType` |
| `SelectStarfighters` | 0 or more | `SelectManufacturableType` |
| `SelectRegiments` | 0 or more | `SelectManufacturableType` |
| `SelectBuildings` | 0 or more | `SelectBuildingsType` |
| `SelectRandom` | 0 or more | `SelectRandomMovableType` |
| `SelectBinding` | 0 or more | `SelectBindingType` |

### `OfficerActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

### `OfficerConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

### `OfficerForceRankConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Rank` | Yes | `None`, `Novice`, `Trainee`, `ForceStudent`, `ForceKnight`, `ForceMaster` |

### `OfficerVoiceType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Path` | No | `xs:string`; minLength `1` |
| `Preset` | No | `Order`, `PersonnelArrived`, `MissionSuccess`, `MissionFailure`, `MissionAbort`, `Released`, `Recovered`, `EnemyDetected`, `ForceGrowth`, `ForceUserDiscovered`, `TraitorDiscovered`, `RescueAttempt` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Path` | Optional | `xs:string`; minLength `1` |
| `Preset` | Optional | `Order`, `PersonnelArrived`, `MissionSuccess`, `MissionFailure`, `MissionAbort`, `Released`, `Recovered`, `EnemyDetected`, `ForceGrowth`, `ForceUserDiscovered`, `TraitorDiscovered`, `RescueAttempt` |

### `PathListType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Path` | 0 or more | `xs:string`; minLength `1` |

### `PerformSkillCheckActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `Rating` | Yes | `Diplomacy`, `Espionage`, `Combat`, `Leadership`, `ShipResearch`, `TroopResearch`, `FacilityResearch` |
| `ProbabilityTable` | Yes | `xs:string`; minLength `1` |
| `RatingMultiplier` | No | `xs:int` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `OnSuccess` | Optional | `ActionsType` |
| `OnFailure` | Optional | `ActionsType` |

### `PlanetStatReferenceType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Name` | Yes | `RawResourceNodes`, `EnergyCapacity` |

### `PopularSupportRollConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `FactionInstanceID` | Yes | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |

### `RandomActionType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Outcomes` | Exactly one | Inline: `Outcome` (1 or more) |

### `RandomOutcomeType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Weight` | Yes | `xs:positiveInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `When` | Optional | `ConditionalsType` |
| `Actions` | Exactly one | `ActionsType` |

### `RandomTickRangeType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `MinimumTicks` | Yes | `xs:positiveInteger` |
| `MaximumTicks` | Yes | `xs:positiveInteger` |

### `RecordPlanetIncidentActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Type` | Yes | `xs:string`; minLength `1` |

### `ReducePlanetStatsActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `LossProbabilityPerResource` | Yes | `xs:decimal`; minInclusive `0`; maxInclusive `1` |
| `MinimumTotalLoss` | Yes | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Stats` | Exactly one | Inline: `Stat` (1 or more) |

### `RevealToFactionActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `FactionInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Subjects` | Exactly one | Inline: `SelectPlanets` (Exactly one), `SelectPlanetSystems` (Exactly one), `SelectFleets` (Exactly one), `SelectMissions` (Exactly one), `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectManufacturingOrders` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `SelectAncestorsType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Type` | Yes | `Galaxy`, `PlanetSystem`, `Planet`, `Fleet`, `Mission`, `CapitalShip` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectPlanets` (Exactly one), `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectMissions` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `SelectBindingType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Binding` | Yes | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |

### `SelectBuildingsType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `TypeID` | No | `xs:string`; minLength `1` |
| `ManufacturingStatus` | No | `Building`, `Complete` |
| `Category` | No | `Any`, `PlanetaryDefense`, `ManufacturingFacility` |

### `SelectFirstDestinationType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectPlanets` (Exactly one), `SelectFleets` (Exactly one), `SelectMissions` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectBinding` (Exactly one), `SelectAncestors` (Exactly one), `SelectPreviousLocation` (Exactly one) |

### `SelectManufacturableType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `TypeID` | No | `xs:string`; minLength `1` |
| `ManufacturingStatus` | No | `Building`, `Complete` |

### `SelectManufacturingOrdersType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `ManufacturingType` | No | `Ship`, `Building`, `Troop` |

### `SelectOfficersType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `PlanetInstanceID` | No | `xs:string`; minLength `1` |
| `PlanetBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `IsCaptured` | No | `xs:boolean` |
| `IncludeRetained` | No | `xs:boolean` |

### `SelectPlanetSystemsType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `SystemType` | No | `CoreSystem`, `OuterRim` |

### `SelectPlanetsType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `InstanceID` | No | `xs:string`; minLength `1` |
| `OwnerFactionInstanceID` | No | `xs:string`; minLength `1` |
| `SystemType` | No | `CoreSystem`, `OuterRim` |

### `SelectPreviousLocationType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | No | `xs:string`; minLength `1` |
| `UnitBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |

### `SelectRandomMovableType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `ChancePercent` | No | `xs:integer`; minInclusive `0`; maxInclusive `100` |
| `Count` | No | `xs:nonNegativeInteger` |
| `MinimumCount` | No | `xs:nonNegativeInteger` |
| `MaximumCount` | No | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |

### `SelectRandomOfficersType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `ChancePercent` | No | `xs:integer`; minInclusive `0`; maxInclusive `100` |
| `Count` | No | `xs:nonNegativeInteger` |
| `MinimumCount` | No | `xs:nonNegativeInteger` |
| `MaximumCount` | No | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectOfficers` (Exactly one), `SelectBinding` (Exactly one) |

### `SelectRandomPlanetsType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `ChancePercent` | No | `xs:integer`; minInclusive `0`; maxInclusive `100` |
| `Count` | No | `xs:nonNegativeInteger` |
| `MinimumCount` | No | `xs:nonNegativeInteger` |
| `MaximumCount` | No | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectPlanets` (Exactly one), `SelectBinding` (Exactly one) |

### `SelectRandomType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `ChancePercent` | No | `xs:integer`; minInclusive `0`; maxInclusive `100` |
| `Count` | No | `xs:nonNegativeInteger` |
| `MinimumCount` | No | `xs:nonNegativeInteger` |
| `MaximumCount` | No | `xs:nonNegativeInteger` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectPlanets` (Exactly one), `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectMissions` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectManufacturingOrders` (Exactly one), `SelectBinding` (Exactly one) |

### `SendMessageActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `RecipientFactionInstanceID` | No | `xs:string`; minLength `1` |
| `RecipientUnitInstanceID` | No | `xs:string`; minLength `1` |
| `SubjectInstanceID` | No | `xs:string`; minLength `1` |
| `SubjectBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `RelatedSubjectInstanceID` | No | `xs:string`; minLength `1` |
| `LocationInstanceID` | No | `xs:string`; minLength `1` |
| `LocationBinding` | No | `xs:string`; pattern `[$][A-Za-z_][A-Za-z0-9_]*` |
| `Type` | No | `PopularSupport`, `Fleet`, `Mission`, `Resource`, `Manufacturing`, `Defense`, `Conflict`, `Chat`, `Advice` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Subject` | Optional | `xs:string` |
| `Body` | Optional | `xs:string` |
| `ConditionalBodies` | Optional | `ConditionalMessageBodyListType` |
| `BackgroundImage` | Optional | `BackgroundImageType` |
| `OverlayImage` | Optional | `MessageImageType` |
| `BackgroundAudio` | Optional | `MessageAudioType` |
| `OfficerVoice` | Optional | `OfficerVoiceType` |
| `AdvisorNotification` | Optional | `AdvisorNotificationType` |

### `SetCaptureStatusActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | No | `xs:string`; minLength `1` |
| `IsCaptured` | Yes | `xs:boolean` |
| `CaptorFactionInstanceID` | No | `xs:string`; minLength `1` |
| `CanEscape` | No | `xs:boolean` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Officers` | Optional | Inline: `SelectOfficers` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `SetEventVariableActionType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Key` | Exactly one | `xs:string`; minLength `1` |
| `Operation` | Optional | `Set`, `Add`, `Minimum`, `Maximum` |
| `Operand` | Exactly one | `xs:integer` |

### `SetOfficerImagesActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `DisplayImagePath` | Optional | `xs:string`; minLength `1` |
| `SmallDisplayImagePath` | Optional | `xs:string`; minLength `1` |
| `MessageImagePath` | Optional | `xs:string`; minLength `1` |
| `EncyclopediaImagePath` | Optional | `xs:string`; minLength `1` |

### `SetOfficerVoiceSetActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `OfficerInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Order` | Optional | `PathListType` |
| `PersonnelArrived` | Optional | `PathListType` |
| `MissionSuccess` | Optional | `PathListType` |
| `MissionFailure` | Optional | `PathListType` |
| `MissionAbort` | Optional | `PathListType` |
| `Released` | Optional | `PathListType` |
| `Recovered` | Optional | `PathListType` |
| `EnemyDetected` | Optional | `PathListType` |
| `ForceGrowth` | Optional | `PathListType` |
| `ForceUserDiscovered` | Optional | `PathListType` |
| `TraitorDiscovered` | Optional | `PathListType` |
| `RescueAttempt` | Optional | `PathListType` |

### `SharedAncestorConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Type` | Yes | `Galaxy`, `PlanetSystem`, `Planet`, `Fleet`, `Mission`, `CapitalShip` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Units` | Exactly one | Inline: `Unit` (2 or more) |

### `SharedNodeConditionalType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Units` | Exactly one | Inline: `Unit` (2 or more) |

### `StringListConditionalType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `UnitInstanceIDs` | Exactly one | `StringListType` |

### `StringListType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `String` | 0 or more | `xs:string`; minLength `1` |

### `TargetType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Candidates` | Exactly one | Inline: `SelectPlanets` (Exactly one), `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectMissions` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectManufacturingOrders` (Exactly one), `SelectRandom` (Exactly one), `SelectBinding` (Exactly one) |

### `TickCountConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Comparison` | Yes | `Equal`, `NotEqual`, `GreaterThan`, `GreaterThanOrEqual`, `LessThan`, `LessThanOrEqual` |
| `Ticks` | Yes | `xs:nonNegativeInteger` |

### `TriggerBindingType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Argument` | Yes | `xs:string`; minLength `1` |
| `As` | Yes | `xs:string`; minLength `1` |

### `TriggerBindingsType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Bind` | 1 or more | `TriggerBindingType` |

### `TriggerDuelActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `FirstOfficerInstanceID` | Yes | `xs:string`; minLength `1` |
| `SecondOfficerInstanceID` | Yes | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `ImagePath` | Optional | `xs:string`; minLength `1` |
| `AudioPath` | Optional | `xs:string`; minLength `1` |

### `TriggerType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `Event` | Yes | `core:unit.arrived`, `core:duel.completed`, `core:officer.capture-changed`, `core:mission.completed`, `core:force.discovered` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Bindings` | Optional | `TriggerBindingsType` |

### `TriggersType`

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Trigger` | 1 or more | `TriggerType` |

### `UnitConditionalType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | Yes | `xs:string`; minLength `1` |

### `UnitTransferActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | No | `xs:string`; minLength `1` |
| `DestinationInstanceID` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `Units` | Optional | `MovableSelectorsType` |
| `Destination` | Optional | `DestinationSelectorsType` |

### `VoidTransferActionType`

Attributes:

| Name | Required | Type or allowed values |
| --- | --- | --- |
| `UnitInstanceID` | No | `xs:string`; minLength `1` |

Children:

| Element | Occurrence | Type |
| --- | --- | --- |
| `UnitInstanceID` | Optional | `xs:string`; minLength `1` |
| `Units` | Optional | Inline: `SelectOfficers` (Exactly one), `SelectSpecialForces` (Exactly one), `SelectFleets` (Exactly one), `SelectCapitalShips` (Exactly one), `SelectStarfighters` (Exactly one), `SelectRegiments` (Exactly one), `SelectBuildings` (Exactly one), `SelectBinding` (Exactly one) |
