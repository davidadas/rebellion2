namespace Rebellion.Game.Traits
{
    /// <summary>
    /// Defines how an authored modifier changes the value accumulated before it.
    /// </summary>
    public enum ModifierOperation
    {
        Multiply,
        Add,
        Override,
    }
}
