/// <summary>
/// Identifies the top-level pages of the in-game Options overlay.
/// </summary>
public enum OptionsMenuTab
{
    /// <summary>Display and detail settings.</summary>
    Graphics = 0,

    /// <summary>Volume settings.</summary>
    Audio = 1,

    /// <summary>Save and load slots.</summary>
    SaveLoad = 2,

    /// <summary>The read-only key-binding reference.</summary>
    Controls = 3,
}
